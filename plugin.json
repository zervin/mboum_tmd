{
  "version": 1,
  "uuid": "mboum-financial-data",
  "iconURL": "https://mboum.com/favicon.ico",
  "emoji": "📈",
  "title": "MBOUM Financial Data",
  "userSettings": {
    "mboumApiKey": {
      "name": "mboumApiKey",
      "description": "MBOUM API Key",
      "type": "string",
      "required": true
    }
  },
  "openaiSpec": {
    "name": "mboum_financial_data",
    "description": "Access MBOUM's comprehensive financial market data API",
    "parameters": {
      "type": "object",
      "required": ["requests"],
      "properties": {
        "requests": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["endpoint", "params"],
            "properties": {
              "endpoint": {
                "type": "string",
                "enum": ["quotes", "historical", "news", "crypto", "calendar", "options", "sectors", "earnings", "dividends", "indices", "holders"],
                "description": "MBOUM API endpoint to call"
              },
              "params": {
                "type": "object",
                "description": "Parameters for the selected endpoint"
              }
            }
          },
          "description": "An array of data requests to the MBOUM API"
        }
      }
    }
  },
  "implementationType": "javascript",
  "id": "mboum_financial_data",
  "method": "GET",
  "outputType": "render_html"
}
