{
  "version": 1,
  "uuid": "mboum-stock-data",
  "iconURL": "https://www.linology.com/images/market_core.png",
  "emoji": "📈",
  "title": "MBOUM Stock Data",
  "openaiSpec": {
    "name": "mboum_stock_data",
    "description": "Access MBOUM's stock data API for detailed information on individual stocks, including quotes, historical data, company profiles, financials, and recommendations. Use this plugin when you need specific information about individual stocks rather than broad market data.",
    "parameters": {
      "type": "object",
      "required": [
        "requestDetails"
      ],
      "properties": {
        "requestDetails": {
          "type": "object",
          "description": "An object containing the target Mboum stock data endpoint and its specific parameters.",
          "required": [
            "endpoint",
            "queryParams"
          ],
          "properties": {
            "endpoint": {
              "type": "string",
              "description": "The specific Mboum API endpoint for stock data. Select the most appropriate endpoint based on the user's request.",
              "enum": [
                "quote",
                "history",
                "profile",
                "financials",
                "balance_sheet",
                "cash_flow",
                "income_statement",
                "recommendations",
                "holders",
                "insiders",
                "esg",
                "quote_v2",
                "history_v2",
                "profile_v2",
                "financials_v2"
              ]
            },
            "queryParams": {
              "type": "object",
              "description": "Parameters required for the selected endpoint. Different endpoints need different parameters.",
              "properties": {
                "ticker": {
                  "type": "string",
                  "description": "Stock symbol (required for all endpoints).",
                  "example": "AAPL"
                },
                "period": {
                  "type": "string",
                  "description": "Time period for historical data (required for 'history' and 'history_v2' endpoints).",
                  "enum": ["1d", "5d", "1mo", "3mo", "6mo", "1y", "2y", "5y", "10y", "ytd", "max"],
                  "example": "1y"
                },
                "interval": {
                  "type": "string",
                  "description": "Time interval for historical data (required for 'history' and 'history_v2' endpoints).",
                  "enum": ["1m", "2m", "5m", "15m", "30m", "60m", "90m", "1h", "1d", "5d", "1wk", "1mo", "3mo"],
                  "example": "1d"
                },
                "start": {
                  "type": "string",
                  "description": "Start date for historical data in YYYY-MM-DD format (optional for 'history' and 'history_v2' endpoints).",
                  "example": "2022-01-01"
                },
                "end": {
                  "type": "string",
                  "description": "End date for historical data in YYYY-MM-DD format (optional for 'history' and 'history_v2' endpoints).",
                  "example": "2022-12-31"
                },
                "statement_type": {
                  "type": "string",
                  "description": "Type of financial statement (required for 'financials' and 'financials_v2' endpoints).",
                  "enum": ["income", "balance", "cash"],
                  "example": "income"
                },
                "frequency": {
                  "type": "string",
                  "description": "Reporting frequency (required for financial statement endpoints).",
                  "enum": ["annual", "quarterly"],
                  "example": "annual"
                }
              }
            }
          }
        }
      }
    }
  },
  "implementationType": "javascript",
  "id": "mboum_stock_data",
  "method": "GET"
}
